/*
 * This file is for easier manual testing of invocations.
 * Comment in the section you care about (or add a new one)
 * and then run node _testscript.js to see the output
 */

var n = require('./lib/n-wrap');
var reg = require('ansi-regex');
//n.use('0.10.41', [process.env.N_PREFIX + '/bin/gulp'], function(err, stdout) {
  //console.log(stdout);
//});

//var res = n.use('0.10.41', [process.env.N_PREFIX + '/bin/gulp']);
//console.log(res);
